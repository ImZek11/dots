![alt text](https://raw.githubusercontent.com/ImZek11/dots11/refs/heads/main/resources/dots11.png)

## RECOMMENDED TO BE DONE ON A:
  Minimal Arch Installation
## how to install:
  **step 1:**
  
  **install git and yay**
  ```
  sudo pacman -Syu --needed git base-devel
  git clone https://aur.archlinux.org/yay.git
  cd yay
  makepkg -si
  ```
  **step 2:**

  **clone the repo**
  ```
  git clone https://ImZek11/dots11.git
  ```
  **step 3:**

  **run the installer**
  ```
  cd dots11/scripts
  chmod +x *
  ./install.sh  
  ```
 **step 4:**

 **Now you can safely reboot, as the installation prompts you when all the steps are completed by your choice!**
